# LangChain ecosystem — v1.x line (create_agent, langchain.agents)
langchain>=1.3
langchain-community>=0.4
langchain-groq>=1.1
langchain-ollama>=1.1
langchain-chroma>=1.1
langgraph>=1.2

# Vector database
chromadb>=1.5

# API framework
fastapi>=0.140
uvicorn[standard]>=0.51

# Observability - OpenTelemetry
opentelemetry-api>=1.44
opentelemetry-sdk>=1.44
opentelemetry-instrumentation-fastapi>=0.65b0
opentelemetry-exporter-otlp>=1.44

# Observability - LangFuse (v3+ API: @observe, langfuse.get_client())
langfuse>=4.14

# Data handling
pydantic>=2.13
python-dotenv>=1.2

# Embeddings
langchain-huggingface>=1.2
sentence-transformers>=5.6

# Document loaders
pypdf>=6.14
beautifulsoup4>=4.15

# HTTP client
httpx>=0.28

# MCP (Model Context Protocol)
mcp>=1.28

# Resource monitoring
psutil>=7.2

# Jupyter notebooks
ipykernel>=7.3
ipywidgets>=8.1

# Utilities
requests>=2.34

# Code formatting. This is the ruff that VS Code actually formats with: the
# extension's importStrategy is "fromEnvironment", so it prefers this one over
# its bundled binary (and falls back to the bundle before setup has run).
# Upper bound (the only one in this file) is deliberate: ruff's formatter output
# changes between minor releases, and an unpinned range would have participants
# reformatting each other's files back and forth.
ruff>=0.16,<0.17
